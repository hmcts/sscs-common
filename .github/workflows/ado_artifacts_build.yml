name: Publish to Azure Artifacts

on:
  workflow_dispatch:
  push:
    branches:
      - '*'
    tags:
      - '*'

jobs:
  PublishToAzureArtifacts:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 17
          cache: 'gradle'

      - name: Get the commits hash
        id: get_versions
        run: |
          sha="${{github.event.pull_request.head.sha}}"
          echo "sha_short=${sha::7}" >> $GITHUB_OUTPUT

      - name: Publish to Azure DevOps Artifacts
        run: |
          echo 'Publishing commit build for SHA Short: "${{ env.RELEASE_VERSION }}"'
          ./gradlew publish
        env:
          AZURE_DEVOPS_ARTIFACT_USERNAME: ${{ secrets.AZURE_DEVOPS_ARTIFACT_USERNAME }}
          AZURE_DEVOPS_ARTIFACT_TOKEN: ${{ secrets.AZURE_DEVOPS_ARTIFACT_TOKEN }}
          RELEASE_VERSION: ${{ steps.get_versions.outputs.sha_short }}
          shell: bash