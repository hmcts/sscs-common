package uk.gov.hmcts.reform.sscs.ccd.event;

import java.util.Set;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.sscs.ccd.domain.SscsCaseData;
import uk.gov.hmcts.reform.sscs.ccd.domain.State;
import uk.gov.hmcts.reform.sscs.ccd.domain.UserRole;
import uk.gov.hmcts.reform.sscs.ccd.event.page.UploadHearingRecordingAddRecordingPage;
import uk.gov.hmcts.reform.sscs.ccd.event.page.UploadHearingRecordingSelectHearingPage;

/**
 * Generated by ccd-definition-converter from Benefit on migration; owned by this service team.
 *
 * <p>Event {@code uploadHearingRecording} for case type {@code Benefit}.
 */
@Component
public class UploadHearingRecording implements CCDConfig<SscsCaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String UPLOAD_HEARING_RECORDING = "uploadHearingRecording";

    /**
     * Registers event {@code uploadHearingRecording}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<SscsCaseData, State, UserRole> builder) {
        var fields = builder.event(UPLOAD_HEARING_RECORDING)
            .forAllStates()
            .name("Upload hearing recording")
            .description("Upload the hearing recording")
            .displayOrder(1770)
            .showSummary()
            .explicitGrants()
            .grant(Permission.CRUD, UserRole.CASEWORKER_SSCS_CLERK, UserRole.CASEWORKER_SSCS_SUPERUSER)
            .grant(Set.of(Permission.R), UserRole.CASEWORKER_SSCS)
            .fields();
        UploadHearingRecordingSelectHearingPage.apply(fields);
        UploadHearingRecordingAddRecordingPage.apply(fields);
    }
}
