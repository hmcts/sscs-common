package uk.gov.hmcts.reform.sscs.ccd.event;

import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.sscs.ccd.domain.CaseOutcome;
import uk.gov.hmcts.reform.sscs.ccd.domain.SscsCaseData;
import uk.gov.hmcts.reform.sscs.ccd.domain.State;
import uk.gov.hmcts.reform.sscs.ccd.domain.UserRole;

/**
 * Generated by ccd-definition-converter from Benefit on migration; owned by this service team.
 *
 * <p>Event {@code addCaseOutcome} for case type {@code Benefit}.
 */
@Component
public class AddCaseOutcome implements CCDConfig<SscsCaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String ADD_CASE_OUTCOME = "addCaseOutcome";

    /**
     * Registers event {@code addCaseOutcome}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<SscsCaseData, State, UserRole> builder) {
        var fields = builder.event(ADD_CASE_OUTCOME)
            .forStates(State.DORMANT_APPEAL_STATE, State.POST_HEARING, State.WITH_UT, State.HEARING, State.READY_TO_LIST)
            .name("Add case outcome")
            .description("Add a case outcome")
            .displayOrder(141)
            .showCondition("caseOutcome=\"DONOTSHOW\"")
            .showEventNotes()
            .explicitGrants()
            .grant(Permission.CRUD, UserRole.CASEWORKER_SSCS, UserRole.CASEWORKER_SSCS_CLERK)
            .fields();
        fields.page("1.0");
        fields.complex(SscsCaseData::getCaseOutcome)
                    .mandatory(CaseOutcome::getCaseOutcome)
                    .mandatory(CaseOutcome::getDidPoAttend).done();
    }
}
