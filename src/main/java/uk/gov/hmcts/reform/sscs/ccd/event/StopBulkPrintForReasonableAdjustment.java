package uk.gov.hmcts.reform.sscs.ccd.event;

import java.util.Set;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.sscs.ccd.domain.SscsCaseData;
import uk.gov.hmcts.reform.sscs.ccd.domain.State;
import uk.gov.hmcts.reform.sscs.ccd.domain.UserRole;

/**
 * Generated by ccd-definition-converter from Benefit on migration; owned by this service team.
 *
 * <p>Event {@code stopBulkPrintForReasonableAdjustment} for case type {@code Benefit}.
 */
@Component
public class StopBulkPrintForReasonableAdjustment implements CCDConfig<SscsCaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String STOP_BULK_PRINT_FOR_REASONABLE_ADJUSTMENT = "stopBulkPrintForReasonableAdjustment";

    /**
     * Registers event {@code stopBulkPrintForReasonableAdjustment}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<SscsCaseData, State, UserRole> builder) {
        builder.event(STOP_BULK_PRINT_FOR_REASONABLE_ADJUSTMENT)
            .forAllStates()
            .name("Stop bulk print")
            .description("Stop a bulk print request for a reasonable adjustment")
            .displayOrder(1520)
            .showEventNotes()
            .explicitGrants()
            .grant(Set.of(Permission.R), UserRole.CASEWORKER_SSCS)
            .grant(Permission.CRUD, UserRole.CASEWORKER_SSCS_SYSTEMUPDATE);
    }
}
