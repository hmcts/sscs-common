package uk.gov.hmcts.reform.sscs.ccd.event;

import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.sscs.ccd.domain.HearingOutcomeDetails;
import uk.gov.hmcts.reform.sscs.ccd.domain.SscsCaseData;
import uk.gov.hmcts.reform.sscs.ccd.domain.State;
import uk.gov.hmcts.reform.sscs.ccd.domain.UserRole;

/**
 * Generated by ccd-definition-converter from Benefit on migration; owned by this service team.
 *
 * <p>Event {@code amendHearingOutcome} for case type {@code Benefit}.
 */
@Component
public class AmendHearingOutcome implements CCDConfig<SscsCaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String AMEND_HEARING_OUTCOME = "amendHearingOutcome";

    /**
     * Registers event {@code amendHearingOutcome}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<SscsCaseData, State, UserRole> builder) {
        var fields = builder.event(AMEND_HEARING_OUTCOME)
            .forAllStates()
            .name("Amend Hearing Outcome")
            .description("Amend a Hearing Outcome")
            .displayOrder(361)
            .showEventNotes()
            .explicitGrants()
            .grant(Permission.CRUD, UserRole.CASEWORKER_SSCS, UserRole.CASEWORKER_SSCS_CLERK)
            .fields();
        fields.page("1.0");
        fields.complex(SscsCaseData::getHearingOutcomes).done();
        fields.complex(SscsCaseData::getHearingOutcomes, HearingOutcomeDetails.class)
                    .mandatory(HearingOutcomeDetails::getHearingOutcomeId)
                    .mandatory(HearingOutcomeDetails::getDidPoAttendHearing)
                    .mandatory(HearingOutcomeDetails::getCompletedHearings).done();
        fields.mandatoryNoSummary(SscsCaseData::getCompletedHearingsList)
                    .fieldShowCondition("hearingOutcomes.hearingOutcomeId=\"DoNotShow\"")
                    .retainHiddenValue();
    }
}
