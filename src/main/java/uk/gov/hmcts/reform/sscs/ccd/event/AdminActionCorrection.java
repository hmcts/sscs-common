package uk.gov.hmcts.reform.sscs.ccd.event;

import java.util.Set;
import org.springframework.stereotype.Component;
import uk.gov.hmcts.ccd.sdk.api.CCDConfig;
import uk.gov.hmcts.ccd.sdk.api.ConfigBuilder;
import uk.gov.hmcts.ccd.sdk.api.Permission;
import uk.gov.hmcts.reform.sscs.ccd.domain.SscsCaseData;
import uk.gov.hmcts.reform.sscs.ccd.domain.State;
import uk.gov.hmcts.reform.sscs.ccd.domain.UserRole;
import uk.gov.hmcts.reform.sscs.ccd.event.page.AdminActionCorrectionPreviewDecisionNoticePage;
import uk.gov.hmcts.reform.sscs.ccd.event.page.AdminActionCorrectionTypeSelectionPage;

/**
 * Generated by ccd-definition-converter from Benefit on migration; owned by this service team.
 *
 * <p>Event {@code adminActionCorrection} for case type {@code Benefit}.
 */
@Component
public class AdminActionCorrection implements CCDConfig<SscsCaseData, State, UserRole> {
    /**
     * The CCD event ID.
     */
    public static final String ADMIN_ACTION_CORRECTION = "adminActionCorrection";

    /**
     * Registers event {@code adminActionCorrection}.
     *
     * @param builder the config builder
     */
    @Override
    public void configure(ConfigBuilder<SscsCaseData, State, UserRole> builder) {
        var fields = builder.event(ADMIN_ACTION_CORRECTION)
            .forStates(State.DORMANT_APPEAL_STATE, State.POST_HEARING, State.WITH_UT)
            .name("Admin - action correction")
            .displayOrder(139)
            .showCondition("hearingRoute=\"listAssist\"")
            .showSummary(false)
            .showEventNotes()
            .explicitGrants()
            .grant(Permission.CRUD, UserRole.CASEWORKER_SSCS_CLERK, UserRole.CASEWORKER_SSCS_SUPERUSER)
            .grant(Set.of(Permission.R), UserRole.CASEWORKER_SSCS)
            .fields();
        AdminActionCorrectionTypeSelectionPage.apply(fields);
        AdminActionCorrectionPreviewDecisionNoticePage.apply(fields);
    }
}
